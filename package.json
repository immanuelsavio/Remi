{
  "name": "remi",
  "version": "0.1.0",
  "description": "A macOS menu-bar day tracker for ADHD brains, built around the time you lose - not just the time you spend.",
  "license": "SEE LICENSE IN LICENSE",
  "author": "Immanuel Savio",
  "homepage": "https://github.com/immanuelsavio/Remi#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/immanuelsavio/Remi.git"
  },
  "bugs": {
    "url": "https://github.com/immanuelsavio/Remi/issues"
  },
  "keywords": [
    "adhd",
    "time-tracking",
    "menu-bar",
    "tauri",
    "svelte",
    "productivity",
    "macos"
  ],
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "//": "The Rust crate lives in src-tauri/; the Tauri CLI finds it automatically.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest run",
    "check": "svelte-check --tsconfig ./tsconfig.json",
    "tauri": "tauri",
    "app": "tauri dev",
    "release": "tauri build",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "npm run check && npm run format:check",
    "verify": "npm run test && npm run check && npm run format:check && npm run build && cargo test --manifest-path src-tauri/Cargo.toml && cargo fmt --manifest-path src-tauri/Cargo.toml --check && cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets --all-features -- -D warnings"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.1.1"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^3.1.2",
    "@tauri-apps/cli": "^2.1.0",
    "@tsconfig/svelte": "^5.0.4",
    "jsdom": "^24.1.3",
    "prettier": "^3.9.6",
    "prettier-plugin-svelte": "^3.5.2",
    "svelte": "^4.2.19",
    "svelte-check": "^3.8.6",
    "tslib": "^2.7.0",
    "typescript": "^7.0.2",
    "vite": "^5.4.10",
    "vitest": "^1.6.1"
  },
  "allowScripts": {
    "esbuild@0.21.5": true
  }
}
