<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Extra Info.plist keys, merged into the generated macOS bundle. Tauri picks
  this up automatically because it sits beside tauri.conf.json - no config
  entry needed.

  LSUIElement marks Remi as an "agent" app: menu bar only, no Dock icon and
  no app-switcher entry. main.rs also calls ActivationPolicy::Accessory, but
  that runs AFTER launch, so without this key the Dock icon visibly flashes
  on every start. Both are needed; neither replaces the other.
-->
<plist version="1.0">
  <dict>
    <key>LSUIElement</key>
    <true/>
  </dict>
</plist>
